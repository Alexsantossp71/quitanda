/// Controls whether the app runs in demo (offline) mode.
///
/// When `true`, no HTTP requests are made to the backend.
/// All data comes from local mock providers.
/// Toggle this to `false` to connect to the real Back4App API.
const bool kDemoMode = true;
